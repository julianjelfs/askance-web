{
  "format_version": 2,
  "sksl": {
    "entrypoint": "kuwahara_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec2 uSize;\nuniform float uRadius;\nuniform shader uSrc;\nuniform half2 uSrc_size;\n\nvec4 fragColor;\n\nvec2 FLT_flutter_local_FlutterFragCoord()\n{\n    return flutter_FragCoord.xy;\n}\n\nfloat FLT_flutter_local_luma(vec3 c)\n{\n    return dot(c, vec3(0.2989999949932098388671875, 0.58700001239776611328125, 0.114000000059604644775390625));\n}\n\nvoid FLT_main()\n{\n    vec2 texel = vec2(1.0) / uSize;\n    vec2 here = FLT_flutter_local_FlutterFragCoord();\n    vec3 flattest = uSrc.eval(uSrc_size * ( here * texel)).xyz;\n    float lowest = 1000000000.0;\n    for (int q = 0; q < 4; q++)\n    {\n        vec2 towards = vec2(((q == 0) || (q == 3)) ? (-1.0) : 1.0, (q < 2) ? (-1.0) : 1.0);\n        vec3 sum = vec3(0.0);\n        float sumSquares = 0.0;\n        float count = 0.0;\n        for (int i = 0; i <= 6; i++)\n        {\n            if (float(i) > uRadius)\n            {\n                continue;\n            }\n            for (int j = 0; j <= 6; j++)\n            {\n                if (float(j) > uRadius)\n                {\n                    continue;\n                }\n                vec2 at = here + vec2(float(i) * towards.x, float(j) * towards.y);\n                vec3 c_1 = uSrc.eval(uSrc_size * ( clamp(at * texel, vec2(0.0), vec2(1.0)))).xyz;\n                sum += c_1;\n                vec3 param = c_1;\n                float l = FLT_flutter_local_luma(param);\n                sumSquares += (l * l);\n                count += 1.0;\n            }\n        }\n        vec3 mean = sum / vec3(count);\n        vec3 param_1 = mean;\n        float meanLuma = FLT_flutter_local_luma(param_1);\n        float variance = (sumSquares / count) - (meanLuma * meanLuma);\n        if (variance < lowest)\n        {\n            lowest = variance;\n            flattest = mean;\n        }\n    }\n    fragColor = vec4(flattest, 1.0);\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 0,
        "name": "uSize",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 1,
        "name": "uRadius",
        "rows": 1,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 0,
        "columns": 1,
        "location": 2,
        "name": "uSrc",
        "rows": 1,
        "type": 12
      }
    ]
  }
}